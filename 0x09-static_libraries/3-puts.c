#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * _puts - writes strings to the output stream
 *
 * Return:Always 0 (success)
 */
int main(void)
{

	puts(" \"programming is like building a multilingual puzzle");
	return (0);
}
